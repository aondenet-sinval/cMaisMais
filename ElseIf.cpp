#include <iostream>

using namespace std;

int main(){
	int x, d, e;
	x = 2;
	d = 4;
	e = 40;
	if(x != 2)
		cout << "x não é diferente de 2";
		else if(d != 4)
			cout << "d não é diferente de 4";
			else if(e == 40)
				cout << "Teste retornou true";
	else
		cout << "Nenhuma das opções é diferente das opções numéricas" ;								
	cout << endl;
}
