#include <iostream>
using namespace std;
// prototipo da funcao que dada a data, retorna o dia da semana.
//seg=0, ter=1, qua=2, qui=3, sex=4, sab=5, dom=6
enum diaDaSemana {SEG, TER, QUA, QUI, SEX, SAB, DOM};

enum diaDaSemana diaMes( int dia, int mes, int ano );
int main(){
int diaPgto, mesPgto, anoPgto;
int diaSem;
string inseto1 = "abelha";
			
	cout << "Numero de caracteres de " << inseto1 << ": " << inseto1.length() << endl;
	cout << "Numero de caracteres de " << inseto1 << ": " << inseto1.size() << endl;
	
	//Trocando caracter da string abelha
	inset01[5] = "o";
	cout << "Numero de caracteres de " << inseto1 << ": " << inseto1.size() << endl;
}
