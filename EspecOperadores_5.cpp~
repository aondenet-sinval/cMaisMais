#include <iostream>
using namespace std;
// prototipo da funcao que dada a data, retorna o dia da semana.
//seg=0, ter=1, qua=2, qui=3, sex=4, sab=5, dom=6
enum diaDaSemana {SEG, TER, QUA, QUI, SEX, SAB, DOM};

enum diaDaSemana diaMes( int dia, int mes, int ano );
int main(){
int diaPgto, mesPgto, anoPgto;
int diaSem;
int as = 20;
as = 10;
/*
	cout << "Entre com a data de pagamento (dd mm aa): ";
	cin >> diaPgto >> mesPgto >> anoPgto;
	//diaSem = diaMes( diaPgto, mesPgto, anoPgto );
		if( diaSem == 5 )
			diaPgto = diaPgto + 2;
		else if( diaSem == 6 )
			diaPgto++;
			
	cout << "Data do pagamento: " << diaPgto << "/" << mesPgto << "/"	<< anoPgto << endl;
*/
}
